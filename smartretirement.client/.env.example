# Leave blank for same-origin /api requests. During local development, Vite
# forwards those requests using DEV_API_TARGET below.
VITE_API_BASE_URL=

# Used only by the local Vite proxy; it is not exposed to browser code.
DEV_API_TARGET=http://localhost:5045
